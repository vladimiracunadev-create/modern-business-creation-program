# Dependencias del repositorio, fijadas a versión exacta.
#
# Los generadores del currículo, del sitio y los validadores usan solo la
# biblioteca estándar. La única dependencia de terceros es la compilación del
# manual en PDF, que corre en scripts/generar_manual.py.
#
# Instalación:  python -m pip install -r requirements.txt

reportlab==4.4.10
pypdf==6.4.0
